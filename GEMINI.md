# GEMINI.md

## Project Overview
This is a new project named **chatbot**. Based on the initial setup, it appears to be a development environment for building a chatbot or AI-integrated application.

### Main Technologies
- **AI Integration:** Likely Anthropic (inferred from `.env` content).
- **Environment Management:** Uses `.env` for configuration.
- **Source Control:** Git (includes a basic `.gitignore`).

## Building and Running
As of now, the project does not contain source files or a build system.
- **TODO:** Document commands for installing dependencies (e.g., `npm install`, `pip install`).
- **TODO:** Document commands for starting the application.
- **TODO:** Document commands for running tests.

## Development Conventions
- **Configuration:** Sensitive information and API keys must be stored in the `.env` file and never committed to source control.
- **TODO:** Establish coding styles and linting rules.
- **TODO:** Define testing practices.

## Directory Structure
- `.env`: Local environment variables (ignored by git).
- `.gitignore`: Specifies files and directories to be ignored by git.
- `GEMINI.md`: This file, providing context and instructions for AI interactions.
